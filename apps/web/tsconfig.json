{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["../../src/*"],
      "@packages/*": ["../../packages/*"]
    }
  },
  "include": [
    "../../next-env.d.ts",
    "../../src/**/*.ts",
    "../../src/**/*.tsx",
    "../../next.config.ts",
    "../../app.config.ts"
  ],
  "exclude": [
    "../../node_modules",
    "../../.next",
    "../../dist",
    "../../build"
  ]
}